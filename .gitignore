# secrets
.env
.env.*
!.env.example

# factory stop button (local kill file, never committed)
.factory-stop

# node / bun
node_modules/
bun.lock.local

# python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# harness artifacts
harness/.artifacts/

# OS
.DS_Store

# local Claude Code session runtime state
.claude/scheduled_tasks.lock

# Unreal project (app/) — a local-machine symlink to the real project on the Windows
# filesystem (see scripts/link-unreal-project.sh and CLAUDE.md's Environment section),
# not repo content. It failed to open via a WSL-hosted copy (Unreal couldn't open the
# .uproject across the WSL<->Windows network-share boundary — .factory/decisions.md
# D-003), so it stays external and gitignored rather than git-tracked.
/app
