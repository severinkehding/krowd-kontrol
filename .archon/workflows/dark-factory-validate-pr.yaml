name: dark-factory-validate-pr
description: |
  Use when: Dark Factory orchestrator wants to validate a PR labeled `factory:needs-review`
            against its linked issue, run the harness gate + four parallel HOLDOUT
            reviewers (behavioral, agent-browser E2E, security, code review) under the
            holdout pattern, optionally self-fix issues in a fresh-context session,
            re-validate, and render a final verdict.
  Triggers: Orchestrator dispatch for `factory:needs-review`, or manual "validate PR #N".
  Does: Fetches PR + linked issue + base-branch governance -> runs the harness gate
        (harness/ci.py, stack-agnostic) -> runs static/holdout reviewers in parallel ->
        synthesizes a verdict. If request_changes, runs a fresh-context fix pass, then a
        second full validation pass (all reviewers re-run on the updated diff). Always
        tears down, then applies the final verdict (approve / request_changes / reject).
  NOT for: Triage (dark-factory-triage), initial implementation
           (dark-factory-fix-github-issue), weekly full-suite E2E
           (dark-factory-comprehensive-test, currently parked - see FACTORY.md).

  Combines what used to be two workflows (validate-pr + fix-pr) into one state machine,
  so fix and re-validate happen in the same orchestrator dispatch with different
  fresh-context sessions.

  ═══════════════════════════════════════════════════════════════════════════
  HOLDOUT PRINCIPLE — the core design constraint
  ═══════════════════════════════════════════════════════════════════════════

  The validator must NEVER see implementation artifacts or coder rationale. It
  tests the OUTCOME (diff + harness gate + running app where available) against the
  CONTRACT (issue body + base-branch governance). This is the StrongDM holdout pattern
  — the single most important defense against AI coding agents gaming their own
  quality signal.

  How the holdout is enforced in this workflow (six overlapping layers):

  1. Cross-workflow isolation — runs as a separate Archon workflow with its own
     $ARTIFACTS_DIR and worktree. Never references sibling-workflow state.

  2. Holdout-safe fetches — gh pr view excludes `comments`/`reviews`; gh issue
     view fetches only title/body/labels. No coder chatter reaches the reviewers.

  3. Poison immunity — FACTORY_RULES.md, MISSION.md, CLAUDE.md are fetched from
     `origin/main` BEFORE `gh pr checkout`. A PR cannot weaken its own rulebook.
     Any diff touching governance files is an automatic REJECT.

  4. Fresh context + empty tool lists on every AI reviewer node. The behavioral
     static reviewer has `allowed_tools: []`. The E2E reviewer has `[Bash]` (only
     to drive agent-browser, currently unreachable — see harness/decisions.md D-001)
     and is explicitly forbidden from reading source.

  5. Command-file explicit prohibitions — every AI node loads a .md command file
     whose leading section enumerates what must not be read (plans, commit
     messages, coder rationale, prior comments, sibling-workflow files).

  6. Fix/validate session isolation — the fix phase runs in a fresh context. When
     pass-2's validators re-run, they ALSO run in fresh contexts. The fixer's
     reasoning never leaks into the second validation pass.

  KNOWN GAP (see .factory/decisions.md D-005; D-001's original "no app to connect to"
  half is resolved — harness/ci.py full mode genuinely passes now, GATE_OK against a
  real Unreal Automation Framework test): the E2E holdout node has no MCP tool access
  and no guaranteed live Editor session for unattended dispatch, so it cannot
  independently observe the game the way a browser-driven holdout could for a web
  app. `behavioral-e2e` reports this honestly rather than faking a pass, and the
  synthesizer's approve rules require `app_booted == true` unconditionally, so this
  gap blocks auto-merge rather than silently bypassing it.

provider: claude
model: sonnet

nodes:
  # ═══════════════════════════════════════════════════════════════
  # PHASE 1: EXTRACT + HOLDOUT-SAFE CONTEXT FETCH
  # ═══════════════════════════════════════════════════════════════

  - id: extract-pr-number
    command: dark-factory-extract-pr-number
    model: haiku
    allowed_tools: [Bash]

  - id: fetch-pr
    bash: |
      set -euo pipefail
      PR_NUMBER=$(echo $extract-pr-number.output | tr -d "'\"\`\n " | grep -oE '[0-9]+' | head -1)
      if [ -z "$PR_NUMBER" ]; then
        RAW_OUTPUT=$extract-pr-number.output
        echo "ERROR: Failed to extract PR number from: $RAW_OUTPUT" >&2
        exit 1
      fi
      echo "$PR_NUMBER" > "$ARTIFACTS_DIR/.pr-number"
      # HOLDOUT: fetch only fields the validator needs.
      # NO comments, NO reviews, NO commit messages.
      gh pr view "$PR_NUMBER" --json number,title,body,url,headRefName,baseRefName,additions,deletions,changedFiles,state,author,labels
    depends_on: [extract-pr-number]
    timeout: 20000

  - id: fetch-diff
    bash: |
      set -euo pipefail
      PR_NUMBER=$(cat "$ARTIFACTS_DIR/.pr-number")
      # Cap at 3000 lines — FACTORY_RULES.md §2 already caps PRs at 500 lines
      # changed; the 3000-line cap here is a defense against accidental huge diffs.
      gh pr diff "$PR_NUMBER" | head -3000
    depends_on: [fetch-pr]
    timeout: 20000

  - id: fetch-linked-issue
    bash: |
      set -euo pipefail
      PR_NUMBER=$(cat "$ARTIFACTS_DIR/.pr-number")
      ISSUE_NUM=$(gh pr view "$PR_NUMBER" --json body -q '.body' \
        | grep -oiE '(fixes|closes|resolves) #[0-9]+' \
        | grep -oE '[0-9]+' | head -1)
      if [ -n "$ISSUE_NUM" ]; then
        # HOLDOUT: title + body + labels only. NO comments.
        gh issue view "$ISSUE_NUM" --json number,title,body,labels,state,url
      else
        echo '{"error": "No linked issue found in PR body. Factory PRs must link via Fixes/Closes/Resolves per FACTORY_RULES.md."}'
      fi
    depends_on: [fetch-pr]
    timeout: 20000

  - id: fetch-base-governance
    bash: |
      set -euo pipefail
      # POISON IMMUNITY: read governance files from origin/main, NOT from the PR.
      git fetch origin main --quiet 2>/dev/null || true
      BASE_REF="origin/main"
      echo "=== FACTORY_RULES.md (from $BASE_REF) ==="
      git show "$BASE_REF:FACTORY_RULES.md" 2>/dev/null || echo "(FACTORY_RULES.md not present on base branch)"
      echo ""
      echo "=== MISSION.md (from $BASE_REF) ==="
      git show "$BASE_REF:MISSION.md" 2>/dev/null || echo "(MISSION.md not present on base branch)"
      echo ""
      echo "=== CLAUDE.md (from $BASE_REF) ==="
      git show "$BASE_REF:CLAUDE.md" 2>/dev/null || echo "(CLAUDE.md not present on base branch)"
    timeout: 15000

  # Tripwire: fail loudly if fix-workflow artifacts ever leak into $ARTIFACTS_DIR.
  # Should be impossible under normal Archon isolation.
  - id: verify-holdout-clean
    bash: |
      set -euo pipefail
      FORBIDDEN_ARTIFACTS=(plan.md investigation.md implementation.md validation.md fix-notes.md)
      LEAKED=()
      for f in "${FORBIDDEN_ARTIFACTS[@]}"; do
        if [ -e "$ARTIFACTS_DIR/$f" ]; then LEAKED+=("$f"); fi
      done
      if [ ${#LEAKED[@]} -gt 0 ]; then
        echo "HOLDOUT VIOLATION: \$ARTIFACTS_DIR contains leaked fix-workflow artifacts: ${LEAKED[*]}" >&2
        echo "This should be impossible under normal Archon isolation. Aborting validation." >&2
        exit 1
      fi
      echo "HOLDOUT_CLEAN: no fix-workflow artifacts in \$ARTIFACTS_DIR"
    timeout: 5000

  - id: checkout-pr
    bash: |
      set -euo pipefail
      PR_NUMBER=$(cat "$ARTIFACTS_DIR/.pr-number")
      # Defensive: drop any stray untracked files in infra directories before
      # switching branches — mirrors dark-factory-experiment's rationale (stale
      # experimental workflow files can otherwise block `gh pr checkout`).
      git clean -fd .archon/ .claude/ 2>/dev/null || true

      # Stale worktree prune — see dark-factory-experiment for the full rationale
      # (a preceding fix-github-issue run can leave a worktree pinned to this
      # PR's branch, which makes `gh pr checkout` fail).
      PR_BRANCH=$(gh pr view "$PR_NUMBER" --json headRefName -q '.headRefName')
      if [ -n "$PR_BRANCH" ]; then
        git worktree prune
        CURRENT_WT=$(git rev-parse --show-toplevel 2>/dev/null || echo "")
        git worktree list --porcelain | awk -v b="$PR_BRANCH" '
          /^worktree / {w=$2}
          $0 == "branch refs/heads/" b {print w}
        ' | while read -r stale_wt; do
          if [ -z "$stale_wt" ] || [ "$stale_wt" = "$CURRENT_WT" ]; then continue; fi
          echo "Removing stale worktree: $stale_wt"
          git worktree remove --force "$stale_wt" 2>&1 || true
        done
        git worktree prune
      fi

      gh pr checkout "$PR_NUMBER"
      echo "Checked out PR #$PR_NUMBER on branch $(git branch --show-current)"
      # Pull infra-only files from origin/main into the worktree so the
      # validator always uses the latest workflow command files and skills,
      # regardless of how stale the PR branch is. Governance was captured into
      # the fetch-base-governance artifact BEFORE this checkout, so the holdout
      # property is preserved either way.
      git fetch origin main --quiet
      git checkout -- .archon/ .claude/ 2>/dev/null || true
      git checkout origin/main -- .archon/ .claude/ 2>/dev/null || true
      echo "Synced .archon/ and .claude/ from origin/main:"
      git status --short .archon/ .claude/ | head -20 || true
    depends_on: [fetch-pr, verify-holdout-clean, fetch-base-governance]
    timeout: 60000

  # app/ is a machine-local symlink (gitignored — CLAUDE.md's Environment section,
  # .factory/decisions.md D-003), so checkout-pr's fresh worktree doesn't have it.
  # Cheap and idempotent; every node touching the Unreal project depends on it.
  - id: link-unreal-project
    bash: |
      set -euo pipefail
      bash scripts/link-unreal-project.sh
    depends_on: [checkout-pr]
    timeout: 10000

  # ═══════════════════════════════════════════════════════════════
  # PHASE 2: THE HARNESS GATE (single entrypoint — see harness/README.md)
  # ═══════════════════════════════════════════════════════════════
  # Unlike dark-factory-experiment (which had six separate hardcoded nodes here:
  # install-deps, start-app, static-checks-backend, static-checks-frontend,
  # run-tests-backend, run-tests-frontend), this repo's gate is ONE stack-agnostic
  # command. harness/ci.py + harness/appproc.py are verbatim/generic; only
  # harness/harness.config.json changes when the stack changes. This is the fix
  # for dark-factory-experiment's own decisions.md D-002 ("two definitions of
  # passing") applied from day one instead of retrofitted later.

  - id: run-harness-p1
    bash: |
      set +e
      python3 harness/ci.py
      RC=$?
      echo ""
      echo "RUN_HARNESS_EXIT=$RC"
      exit $RC
    depends_on: [checkout-pr, link-unreal-project]
    # static + unit + app boot + e2e floor + holdout + mutations, whichever are
    # configured in harness.config.json — give it real headroom.
    timeout: 600000

  # ═══════════════════════════════════════════════════════════════
  # PHASE 3: PASS-1 HOLDOUT REVIEWERS (parallel, fresh context)
  # ═══════════════════════════════════════════════════════════════

  # Static behavioral: reads issue + PR metadata + diff. No filesystem, no tools.
  - id: behavioral-validation-p1
    command: dark-factory-behavioral-validation
    depends_on: [fetch-pr, fetch-diff, fetch-linked-issue]
    context: fresh
    allowed_tools: []
    output_format:
      type: object
      properties:
        solves_issue: { type: string, enum: ["yes", "partially", "no"] }
        asks_identified: { type: array, items: { type: string } }
        asks_addressed:  { type: array, items: { type: string } }
        asks_missed:     { type: array, items: { type: string } }
        regressions_detected: { type: array, items: { type: string } }
        scope_appropriate: { type: string, enum: ["yes", "too_narrow", "too_broad"] }
        unrequested_changes: { type: array, items: { type: string } }
        confidence: { type: string, enum: ["high", "medium", "low"] }
        reasoning: { type: string }
      required:
        - solves_issue
        - asks_identified
        - asks_addressed
        - asks_missed
        - regressions_detected
        - scope_appropriate
        - unrequested_changes
        - confidence
        - reasoning

  # E2E behavioral: currently unreachable pending .factory/decisions.md D-005 (no MCP
  # tool access on this node, no guaranteed live Editor session for unattended
  # dispatch) — reports not_e2e_testable/app_booted honestly. Kept wired (not deleted)
  # so completing D-001 is a small change, not a rebuild.
  - id: behavioral-e2e-p1
    command: dark-factory-behavioral-e2e
    depends_on: [fetch-pr, fetch-linked-issue, run-harness-p1]
    context: fresh
    allowed_tools: [Bash]
    output_format:
      type: object
      properties:
        solves_issue: { type: string, enum: ["yes", "partially", "no", "not_e2e_testable"] }
        app_booted: { type: boolean }
        flows_tested: { type: array, items: { type: string } }
        criteria_results:
          type: array
          items:
            type: object
            properties:
              criterion: { type: string }
              result: { type: string, enum: ["pass", "fail", "skip"] }
              evidence: { type: string }
            required: [criterion, result, evidence]
        regressions_observed: { type: array, items: { type: string } }
        evidence_captured: { type: array, items: { type: string } }
        confidence: { type: string, enum: ["high", "medium", "low"] }
        reasoning: { type: string }
      required:
        - solves_issue
        - app_booted
        - flows_tested
        - criteria_results
        - regressions_observed
        - evidence_captured
        - confidence
        - reasoning

  - id: security-check-p1
    command: dark-factory-security-check
    depends_on: [fetch-diff, fetch-base-governance]
    context: fresh
    allowed_tools: []
    output_format:
      type: object
      properties:
        security_issues:
          type: array
          items:
            type: object
            properties:
              severity: { type: string, enum: ["critical", "high", "medium", "low"] }
              category: { type: string, enum: ["secret", "injection", "dependency", "permission", "governance", "data_exposure"] }
              description: { type: string }
              file: { type: string }
              line: { type: string }
            required: [severity, category, description, file]
        governance_files_modified: { type: boolean }
        protected_files_touched: { type: array, items: { type: string } }
        new_dependencies: { type: array, items: { type: string } }
        new_dependencies_justified: { type: boolean }
        verdict: { type: string, enum: ["pass", "fail"] }
        reasoning: { type: string }
      required:
        - security_issues
        - governance_files_modified
        - protected_files_touched
        - new_dependencies
        - new_dependencies_justified
        - verdict
        - reasoning

  - id: code-review-p1
    command: archon-code-review-agent
    depends_on: [checkout-pr]
    context: fresh

  # ═══════════════════════════════════════════════════════════════
  # PHASE 4: SYNTHESIZE PASS-1 VERDICT
  # ═══════════════════════════════════════════════════════════════

  - id: synthesize-verdict-pass-1
    command: dark-factory-synthesize-verdict
    depends_on:
      - checkout-pr
      - run-harness-p1
      - behavioral-validation-p1
      - behavioral-e2e-p1
      - security-check-p1
      - code-review-p1
      - fetch-base-governance
    trigger_rule: all_done
    context: fresh
    allowed_tools: []
    output_format:
      type: object
      properties:
        verdict: { type: string, enum: ["approve", "request_changes", "reject"] }
        summary: { type: string }
        static_checks_status: { type: string, enum: ["pass", "fail"] }
        tests_status: { type: string, enum: ["pass", "fail", "skipped"] }
        behavioral_status: { type: string, enum: ["yes", "partially", "no"] }
        e2e_status: { type: string, enum: ["yes", "partially", "no", "not_e2e_testable"] }
        security_status: { type: string, enum: ["pass", "fail"] }
        issues_to_fix:
          type: array
          items:
            type: object
            properties:
              category: { type: string, enum: ["behavioral", "test_failure", "static_check", "code_quality", "security", "scope", "e2e"] }
              severity: { type: string, enum: ["critical", "high", "medium", "low"] }
              description: { type: string }
              file: { type: string }
            required: [category, severity, description]
        should_escalate: { type: boolean }
        escalation_reason: { type: string }
        reasoning: { type: string }
      required:
        - verdict
        - summary
        - static_checks_status
        - tests_status
        - behavioral_status
        - e2e_status
        - security_status
        - issues_to_fix
        - should_escalate
        - reasoning

  # ═══════════════════════════════════════════════════════════════
  # PHASE 5: FIX (conditional — runs only when pass-1 says request_changes)
  # ═══════════════════════════════════════════════════════════════

  - id: fix-issues
    command: dark-factory-fix-pr-issues
    depends_on: [synthesize-verdict-pass-1]
    when: "$synthesize-verdict-pass-1.output.verdict == 'request_changes'"
    context: fresh

  # ═══════════════════════════════════════════════════════════════
  # PHASE 6: PASS-2 VALIDATION (conditional — runs only if fix-issues ran)
  # ═══════════════════════════════════════════════════════════════

  - id: fetch-diff-p2
    bash: |
      set -euo pipefail
      PR_NUMBER=$(cat "$ARTIFACTS_DIR/.pr-number")
      git fetch origin "refs/pull/$PR_NUMBER/head" --quiet 2>/dev/null || true
      git pull --ff-only 2>/dev/null || true
      gh pr diff "$PR_NUMBER" | head -3000
    depends_on: [fix-issues]
    timeout: 30000

  - id: run-harness-p2
    bash: |
      set +e
      python3 harness/ci.py
      RC=$?
      echo ""
      echo "RUN_HARNESS_EXIT=$RC"
      exit $RC
    # Same worktree pass-1 already linked app/ in, but idempotent and cheap — no
    # reason to rely on that continuity holding.
    depends_on: [fix-issues, link-unreal-project]
    timeout: 600000

  - id: behavioral-validation-p2
    command: dark-factory-behavioral-validation-p2
    depends_on: [fix-issues, fetch-diff-p2, fetch-pr, fetch-linked-issue]
    context: fresh
    allowed_tools: []
    output_format:
      type: object
      properties:
        solves_issue: { type: string, enum: ["yes", "partially", "no"] }
        asks_identified: { type: array, items: { type: string } }
        asks_addressed:  { type: array, items: { type: string } }
        asks_missed:     { type: array, items: { type: string } }
        regressions_detected: { type: array, items: { type: string } }
        scope_appropriate: { type: string, enum: ["yes", "too_narrow", "too_broad"] }
        unrequested_changes: { type: array, items: { type: string } }
        confidence: { type: string, enum: ["high", "medium", "low"] }
        reasoning: { type: string }
      required:
        - solves_issue
        - asks_identified
        - asks_addressed
        - asks_missed
        - regressions_detected
        - scope_appropriate
        - unrequested_changes
        - confidence
        - reasoning

  - id: behavioral-e2e-p2
    command: dark-factory-behavioral-e2e
    depends_on: [fix-issues, fetch-pr, fetch-linked-issue, run-harness-p2]
    context: fresh
    allowed_tools: [Bash]
    output_format:
      type: object
      properties:
        solves_issue: { type: string, enum: ["yes", "partially", "no", "not_e2e_testable"] }
        app_booted: { type: boolean }
        flows_tested: { type: array, items: { type: string } }
        criteria_results:
          type: array
          items:
            type: object
            properties:
              criterion: { type: string }
              result: { type: string, enum: ["pass", "fail", "skip"] }
              evidence: { type: string }
            required: [criterion, result, evidence]
        regressions_observed: { type: array, items: { type: string } }
        evidence_captured: { type: array, items: { type: string } }
        confidence: { type: string, enum: ["high", "medium", "low"] }
        reasoning: { type: string }
      required:
        - solves_issue
        - app_booted
        - flows_tested
        - criteria_results
        - regressions_observed
        - evidence_captured
        - confidence
        - reasoning

  - id: security-check-p2
    command: dark-factory-security-check-p2
    depends_on: [fix-issues, fetch-diff-p2, fetch-base-governance]
    context: fresh
    allowed_tools: []
    output_format:
      type: object
      properties:
        security_issues:
          type: array
          items:
            type: object
            properties:
              severity: { type: string, enum: ["critical", "high", "medium", "low"] }
              category: { type: string, enum: ["secret", "injection", "dependency", "permission", "governance", "data_exposure"] }
              description: { type: string }
              file: { type: string }
              line: { type: string }
            required: [severity, category, description, file]
        governance_files_modified: { type: boolean }
        protected_files_touched: { type: array, items: { type: string } }
        new_dependencies: { type: array, items: { type: string } }
        new_dependencies_justified: { type: boolean }
        verdict: { type: string, enum: ["pass", "fail"] }
        reasoning: { type: string }
      required:
        - security_issues
        - governance_files_modified
        - protected_files_touched
        - new_dependencies
        - new_dependencies_justified
        - verdict
        - reasoning

  - id: code-review-p2
    command: archon-code-review-agent
    depends_on: [fix-issues]
    context: fresh

  - id: synthesize-verdict-pass-2
    command: dark-factory-synthesize-verdict-p2
    when: "$synthesize-verdict-pass-1.output.verdict == 'request_changes'"
    depends_on:
      - run-harness-p2
      - behavioral-validation-p2
      - behavioral-e2e-p2
      - security-check-p2
      - code-review-p2
      - fetch-base-governance
    trigger_rule: all_done
    context: fresh
    allowed_tools: []
    output_format:
      type: object
      properties:
        verdict: { type: string, enum: ["approve", "request_changes", "reject"] }
        summary: { type: string }
        static_checks_status: { type: string, enum: ["pass", "fail"] }
        tests_status: { type: string, enum: ["pass", "fail", "skipped"] }
        behavioral_status: { type: string, enum: ["yes", "partially", "no"] }
        e2e_status: { type: string, enum: ["yes", "partially", "no", "not_e2e_testable"] }
        security_status: { type: string, enum: ["pass", "fail"] }
        issues_to_fix:
          type: array
          items:
            type: object
            properties:
              category: { type: string, enum: ["behavioral", "test_failure", "static_check", "code_quality", "security", "scope", "e2e"] }
              severity: { type: string, enum: ["critical", "high", "medium", "low"] }
              description: { type: string }
              file: { type: string }
            required: [category, severity, description]
        should_escalate: { type: boolean }
        escalation_reason: { type: string }
        reasoning: { type: string }
      required:
        - verdict
        - summary
        - static_checks_status
        - tests_status
        - behavioral_status
        - e2e_status
        - security_status
        - issues_to_fix
        - should_escalate
        - reasoning

  # ═══════════════════════════════════════════════════════════════
  # PHASE 7: TEARDOWN (always runs)
  # ═══════════════════════════════════════════════════════════════
  # harness/ci.py owns the app's own lifecycle internally (start + teardown in
  # one process, see Phase 2). All that's left here is closing any agent-browser
  # session a reviewer might have opened.

  - id: teardown-app
    bash: |
      set +e
      agent-browser close 2>/dev/null || true
      echo "TEARDOWN_COMPLETE"
    depends_on: [synthesize-verdict-pass-1, synthesize-verdict-pass-2]
    trigger_rule: all_done
    timeout: 30000

  # ═══════════════════════════════════════════════════════════════
  # PHASE 8: APPLY FINAL VERDICT (deterministic bash)
  # ═══════════════════════════════════════════════════════════════
  # Reads whichever synthesize node ran last: if pass-2 ran, use its verdict;
  # otherwise use pass-1. All approve/reject/label operations are pure gh calls
  # — no AI decisions here. Keeps the auto-merge audit trail reproducible.

  - id: apply-verdict
    bash: |
      set -euo pipefail
      PR_NUMBER=$(cat "$ARTIFACTS_DIR/.pr-number")

      cat > "$ARTIFACTS_DIR/p1-verdict.json" <<'__P1_VERDICT_EOF__'
      $synthesize-verdict-pass-1.output
      __P1_VERDICT_EOF__
      cat > "$ARTIFACTS_DIR/p2-verdict.json" <<'__P2_VERDICT_EOF__'
      $synthesize-verdict-pass-2.output
      __P2_VERDICT_EOF__
      for vf in "$ARTIFACTS_DIR/p1-verdict.json" "$ARTIFACTS_DIR/p2-verdict.json"; do
        [ -f "$vf" ] && python3 -c "import sys,pathlib;p=pathlib.Path(sys.argv[1]);t=p.read_text().strip();t=t.strip(chr(39));t=t.replace(chr(39)+chr(92)+chr(39)+chr(39),chr(39));p.write_text(t)" "$vf"
      done

      if [ -s "$ARTIFACTS_DIR/p2-verdict.json" ] && jq -e '.verdict' "$ARTIFACTS_DIR/p2-verdict.json" > /dev/null 2>&1; then
        VERDICT_FILE="$ARTIFACTS_DIR/p2-verdict.json"
        PASS_LABEL="pass-2"
      else
        VERDICT_FILE="$ARTIFACTS_DIR/p1-verdict.json"
        PASS_LABEL="pass-1"
      fi

      if [ ! -s "$VERDICT_FILE" ] || ! jq -e '.verdict' "$VERDICT_FILE" > /dev/null 2>&1; then
        echo "WARN: verdict file ($VERDICT_FILE) is empty or unparseable - emitting fallback reject+escalate" >&2
        printf '%s\n' '{"verdict":"reject","summary":"Validator infrastructure failed - no synthesizer output available. Manual investigation required.","static_checks_status":"fail","tests_status":"fail","behavioral_status":"no","security_status":"fail","issues_to_fix":[],"should_escalate":true,"escalation_reason":"Upstream node (checkout-pr, run-harness, or synthesize-verdict) failed and produced no verdict. Validator cannot render a substantive decision; human must investigate the broken run.","reasoning":"Fallback verdict emitted by apply-verdict because the selected verdict file was empty or unparseable JSON. This indicates an upstream infrastructure failure, not a defect in the PR under review."}' > "$VERDICT_FILE"
      fi

      cp "$VERDICT_FILE" "$ARTIFACTS_DIR/verdict.json"
      VERDICT_JSON=$(cat "$VERDICT_FILE")

      DECISION=$(echo "$VERDICT_JSON" | jq -r '.verdict')
      SUMMARY=$(echo "$VERDICT_JSON" | jq -r '.summary')
      SHOULD_ESCALATE=$(echo "$VERDICT_JSON" | jq -r '.should_escalate')
      ESCALATION_REASON=$(echo "$VERDICT_JSON" | jq -r '.escalation_reason // empty')
      ISSUES=$(echo "$VERDICT_JSON" | jq -r '
        .issues_to_fix
        | map("- **\(.severity)** [\(.category)]\(if .file then " (`\(.file)`)" else "" end): \(.description)")
        | join("\n")')

      # ──────────────────────────────────────────────────────────────────
      # DETERMINISTIC INFRASTRUCTURE BACKSTOP
      # ──────────────────────────────────────────────────────────────────
      # Reads run-harness's own output directly and flips any approve to
      # reject+escalate when GATE_OK is absent. Mirrors dark-factory-experiment's
      # APP_STARTED backstop, generalized to the harness gate's own marker.
      cat > "$ARTIFACTS_DIR/.run-harness-output" <<'__RUN_HARNESS_OUTPUT_EOF__'
      $run-harness-p2.output
      $run-harness-p1.output
      __RUN_HARNESS_OUTPUT_EOF__
      if grep -q "GATE_OK" "$ARTIFACTS_DIR/.run-harness-output"; then
        HARNESS_DID_PASS="true"
      else
        HARNESS_DID_PASS="false"
      fi
      echo "INFRA_BACKSTOP: harness_gate_ok=$HARNESS_DID_PASS decision=$DECISION"

      if [ "$DECISION" = "approve" ] && [ "$HARNESS_DID_PASS" != "true" ]; then
        echo "INFRA_BACKSTOP_OVERRIDE: neither run-harness pass reported GATE_OK but the synthesizer rendered approve — overriding to reject+escalate." >&2
        DECISION="reject"
        SHOULD_ESCALATE="true"
        ESCALATION_REASON="Deterministic infrastructure backstop fired: no run-harness invocation (pass-1 or pass-2) reported GATE_OK, so the deterministic gate never actually passed. The synthesize-verdict node rendered an approve verdict anyway. A human must investigate both the harness failure (check the run-harness node's captured output) and the synthesizer's failure to catch it."
        SUMMARY="Infrastructure backstop override: no harness run reported GATE_OK, so the deterministic gate required by FACTORY_RULES §3 could not have passed. Auto-approve blocked."
        ISSUES=$(printf '%s\n' \
          "- **critical** [static_check]: run-harness node(s) did not report GATE_OK — the harness gate failed or the app could not boot (expected during this repo's bootstrap state — see harness/README.md)." \
          "- **critical** [static_check]: synthesize-verdict rendered approve despite the missing GATE_OK marker — review why rule 0 of the synthesize-verdict command did not fire.")
      fi
      # ──────────────────────────────────────────────────────────────────

      # GitHub forbids reviewing your own PR (addPullRequestReview GraphQL error).
      PR_AUTHOR=$(gh pr view "$PR_NUMBER" --json author -q '.author.login' 2>/dev/null || echo "")
      BOT_LOGIN=$(gh api user -q '.login' 2>/dev/null || echo "")
      SELF_REVIEW="false"
      if [ -n "$PR_AUTHOR" ] && [ "$PR_AUTHOR" = "$BOT_LOGIN" ]; then
        SELF_REVIEW="true"
        echo "Self-review detected (PR author '$PR_AUTHOR' == bot '$BOT_LOGIN'); will post as comment instead of formal review."
      fi

      post_review() {
        local flag="$1"
        local body="$2"
        if [ "$SELF_REVIEW" = "true" ]; then
          gh pr comment "$PR_NUMBER" --body "$body"
        else
          gh pr review "$PR_NUMBER" "$flag" --body "$body"
        fi
      }

      case "$DECISION" in
        approve)
          BODY=$(printf '%s\n' \
            "**Dark Factory Validation**: APPROVED (${PASS_LABEL})" \
            "" \
            "${SUMMARY}" \
            "" \
            "All quality gates passed:" \
            "- Behavioral (static): solves_issue=yes" \
            "- Harness gate: GATE_OK" \
            "- Security: pass (no governance files modified)" \
            "- Code review: no critical/high findings" \
            "" \
            "Auto-merging via squash.")
          post_review --approve "$BODY"
          IS_DRAFT=$(gh pr view "$PR_NUMBER" --json isDraft -q '.isDraft' 2>/dev/null || echo "false")
          if [ "$IS_DRAFT" = "true" ]; then
            echo "PR #$PR_NUMBER is draft — marking ready for review before merge"
            gh pr ready "$PR_NUMBER"
          fi
          if gh pr merge "$PR_NUMBER" --squash; then
            echo "PR #$PR_NUMBER: merged via squash"
          else
            echo "WARN: squash merge failed for PR #$PR_NUMBER — leaving approved for manual merge" >&2
          fi
          gh pr edit "$PR_NUMBER" --remove-label "factory:needs-review" --add-label "factory:approved" 2>/dev/null || true
          echo "PR #$PR_NUMBER: APPROVED (${PASS_LABEL})"
          ;;

        request_changes)
          if [ "$PASS_LABEL" = "pass-2" ] || [ "$SHOULD_ESCALATE" = "true" ]; then
            BODY=$(printf '%s\n' \
              "**Dark Factory Validation**: ESCALATED TO HUMAN (${PASS_LABEL})" \
              "" \
              "${SUMMARY}" \
              "" \
              "### Escalation reason" \
              "${ESCALATION_REASON:-Autonomous fix pass did not resolve all blocking issues. Human review required.}" \
              "" \
              "### Issues remaining" \
              "${ISSUES}")
            post_review --request-changes "$BODY"
            gh pr edit "$PR_NUMBER" --remove-label "factory:needs-review" --add-label "factory:needs-human" 2>/dev/null || true
            echo "PR #$PR_NUMBER: ESCALATED to human after ${PASS_LABEL}"
          else
            BODY=$(printf '%s\n' \
              "**Dark Factory Validation**: Changes Requested (${PASS_LABEL})" \
              "" \
              "${SUMMARY}" \
              "" \
              "### Issues to fix" \
              "${ISSUES}")
            post_review --request-changes "$BODY"
            gh pr edit "$PR_NUMBER" --remove-label "factory:needs-review" --add-label "factory:needs-fix" 2>/dev/null || true
            echo "PR #$PR_NUMBER: CHANGES REQUESTED (${PASS_LABEL})"
          fi
          ;;

        reject)
          if [ "$SHOULD_ESCALATE" = "true" ]; then
            BODY=$(printf '%s\n' \
              "**Dark Factory Validation**: REJECTED + ESCALATED (${PASS_LABEL})" \
              "" \
              "${SUMMARY}" \
              "" \
              "### Escalation reason" \
              "${ESCALATION_REASON}" \
              "" \
              "### Findings" \
              "${ISSUES}" \
              "" \
              "This PR is NOT being closed — a human needs to investigate.")
            post_review --request-changes "$BODY"
            gh pr edit "$PR_NUMBER" --remove-label "factory:needs-review" --add-label "factory:needs-human" 2>/dev/null || true

            ISSUE_NUM=$(gh pr view "$PR_NUMBER" --json body -q '.body' \
              | grep -oiE '(fixes|closes|resolves) #[0-9]+' \
              | grep -oE '[0-9]+' | head -1)
            if [ -n "$ISSUE_NUM" ]; then
              gh issue edit "$ISSUE_NUM" --add-label "factory:needs-human" 2>/dev/null || true
              ICOMMENT=$(printf '%s\n' \
                "**Dark Factory**: PR #$PR_NUMBER was rejected and escalated to human review (${PASS_LABEL})." \
                "" \
                "Reason: ${SUMMARY}" \
                "" \
                "${ESCALATION_REASON}")
              gh issue comment "$ISSUE_NUM" --body "$ICOMMENT"
            fi
            echo "PR #$PR_NUMBER: REJECTED+ESCALATED (${PASS_LABEL}) — left open for human review"
          else
            BODY=$(printf '%s\n' \
              "**Dark Factory Validation**: REJECTED (${PASS_LABEL})" \
              "" \
              "${SUMMARY}" \
              "" \
              "### Blocking issues" \
              "${ISSUES}" \
              "" \
              "This PR cannot be fixed incrementally and has been closed.")
            post_review --request-changes "$BODY"
            gh pr close "$PR_NUMBER"

            ISSUE_NUM=$(gh pr view "$PR_NUMBER" --json body -q '.body' \
              | grep -oiE '(fixes|closes|resolves) #[0-9]+' \
              | grep -oE '[0-9]+' | head -1)
            if [ -n "$ISSUE_NUM" ]; then
              gh issue reopen "$ISSUE_NUM" 2>/dev/null || true
              gh issue edit "$ISSUE_NUM" --add-label "factory:accepted" --remove-label "factory:in-progress" 2>/dev/null || true
              ICOMMENT=$(printf '%s\n' \
                "**Dark Factory**: PR #$PR_NUMBER was rejected during validation (${PASS_LABEL}). Issue re-queued for another implementation attempt." \
                "" \
                "Reason: ${SUMMARY}")
              gh issue comment "$ISSUE_NUM" --body "$ICOMMENT"
            fi
            echo "PR #$PR_NUMBER: REJECTED (${PASS_LABEL}) and closed"
          fi
          ;;

        *)
          echo "ERROR: Unknown verdict '$DECISION' from ${PASS_LABEL}" >&2
          exit 1
          ;;
      esac
    depends_on: [teardown-app]
    timeout: 30000
